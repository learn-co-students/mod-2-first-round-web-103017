
<% if @superhero.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@superhero.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
    <% @superhero.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for @superhero do |f| %>
  <%= f.label :name%>
  <%= f.text_field :name%> <br>

  <%= f.label :super_name %>
  <%= f.text_field :super_name %><br>

  <%= f.label :superpower_id %>
  <%= f.collection_select :superpower_id, @superpowers, :id, :name, {:prompt => 'Please Select'}%><br>

  <%= f.submit %>

<% end %>
